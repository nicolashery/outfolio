.navbar
  .container
    .navbar-inner.group
      .logo.js-home
        a(href='/home') Outfolio
      if authenticated
        ul.nav
          li.js-cards
            a(href='/cards') Cards
        ul.nav.nav-right
          li #{user.name}
          li.js-sign-out
            a(href='/logout') Sign out
      else
        ul.nav.nav-right
          li.js-sign-in
            a(href='/login') Sign in